```c
int main (void){
	scanf("%d", &N);
	for (int i=1; i<=N; i++){
		scanf("%d %d", &chefe[i], &salario[i]);
		addedge(chefe[i], i);
	}
}

//insatisfeitos iniciais
insas 0;
	for (int i=1; i<=N; i++){
		insat = insatisfeito(i);
	}
	printf("%d\n", &insa);
	scanf("%d\n", &A);
	for (int i=0; i<A; i++){
		scanf("%d %d", &quem, &ns);
		insat=atualizasalario(quem, ns),
	}
	

T
int insatisfeito(int p){
	for (list = G->adj[p]; t!=NULL; t=T->next){
		if(salario[t->V]>salario[p]{
			return 1;
		}
	}
	return 0
}

int atualizasalario (int quem, int ns){
int in = 0;
	if (ns>salario[quem]{
		if(insatisfeito[chefe[quem]]>=0){
			if (ns>salario[chefe[quem]]){
				in++, insatisfeito[chefe[q2]]= 1;
			}
		}
		if insatisfeito
}



```